import { BlockingLoader } from "@/components/ui/blocking-loader"

export default function AppLoading() {
  return <BlockingLoader title="Loading page" description="The next view is being prepared." />
}
